export interface UserTypes {
  email: string;
  name: string;
  password: string;
}

export interface ProfileTypes {
  id: string;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
}
